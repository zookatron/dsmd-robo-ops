{
  "scripts": {
    "run": "vite-node index.ts",
    "run-in-docker": "docker run --rm -d -v $(pwd):/app -w /app --name dsmd-robo-ops node sh -c 'yarn && yarn run run'"
  },
  "dependencies": {
    "@types/node": "^20.12.12",
    "dotenv": "^16.4.5",
    "knex": "^3.1.0",
    "pg": "^8.11.5",
    "vite-node": "^1.6.0"
  }
}
